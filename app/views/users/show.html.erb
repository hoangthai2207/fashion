<% provide(:title, @product.name) %>
<div class="row">
    <aside class="col-md-4">
        <section class="product_info">
            <h1>

                <%= @product.name %>
            </h1>
        </section>
    </aside>
    <div class="col-md-8">


        <ol class="microposts">
            <%= render @products %>
        </ol>

        <% end %>
    </div>
</div>
